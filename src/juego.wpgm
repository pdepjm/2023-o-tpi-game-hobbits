import wollok.game.*
import SpaceObjects.*
 
program juego {
	game.title("Space Invader")
	
	game.width(20)
	game.height(10)
	
	game.ground("imagenes_Juego/fondo.png")
	
	//keyboard.up().onPressDo( { nave.moverseHaciaArriba() } )
	//keyboard.down().onPressDo( { nave.moverseHaciaAbajo() } )
	keyboard.left().onPressDo( { nave.moverseHaciaIzquierda() } )
	keyboard.right().onPressDo( { nave.moverseHaciaDerecha() } )
	
	game.addVisual(nave)
	game.addVisual(nave2)
	game.start()
}